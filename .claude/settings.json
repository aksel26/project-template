{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *src/*.ts) pnpm exec tsc --noEmit;; esac; }",
            "timeout": 60,
            "statusMessage": "Type-checking (tsc --noEmit)..."
          }
        ]
      }
    ]
  }
}
